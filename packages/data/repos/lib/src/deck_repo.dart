import 'package:repos/src/models/deck.dart';

class DeckRepo {
  Stream<List<Deck>> getDecks() {
    throw UnimplementedError();
  }

  Future<Deck> addNewDeck(Deck deck) {
    throw UnimplementedError();
  }
}
